# ********RoostGPT********
"""
Test generated by RoostGPT for test PythonTest111 using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=SimpleCalculator.modulus_a78485441a
ROOST_METHOD_SIG_HASH=SimpleCalculator.modulus_36a969db72


Scenario 1: Test with positive integers
Details:
  TestName: test_modulus_positive_integers
  Description: This test is intended to verify the correct calculation of the modulus when both input numbers are positive integers.
Execution:
  Arrange: No specific arrangement is required as the function does not depend on any external state.
  Act: Invoke the function modulus with two positive integers, for example, modulus(10, 3).
  Assert: Check that the returned value is the correct modulus of the two numbers, in this case, 1.
Validation:
  This test is important to ensure that the function correctly handles the most basic and common use case, which is the calculation of the modulus of two positive integers.

Scenario 2: Test with negative integers
Details:
  TestName: test_modulus_negative_integers
  Description: This test is intended to verify the correct calculation of the modulus when both input numbers are negative integers.
Execution:
  Arrange: No specific arrangement is required as the function does not depend on any external state.
  Act: Invoke the function modulus with two negative integers, for example, modulus(-10, -3).
  Assert: Check that the returned value is the correct modulus of the two numbers, in this case, -1.
Validation:
  This test is important to ensure that the function correctly handles the case where both input numbers are negative.

Scenario 3: Test with zero as the second parameter
Details:
  TestName: test_modulus_zero_second_parameter
  Description: This test is intended to verify the correct handling of the case where the second parameter is zero.
Execution:
  Arrange: No specific arrangement is required as the function does not depend on any external state.
  Act: Invoke the function modulus with zero as the second parameter, for example, modulus(10, 0).
  Assert: Check that the returned value is the string "Cannot perform modulus by zero".
Validation:
  This test is important to ensure that the function correctly handles the case where the second parameter is zero, which is not a valid input for a modulus operation.

Scenario 4: Test with zero as the first parameter
Details:
  TestName: test_modulus_zero_first_parameter
  Description: This test is intended to verify the correct handling of the case where the first parameter is zero.
Execution:
  Arrange: No specific arrangement is required as the function does not depend on any external state.
  Act: Invoke the function modulus with zero as the first parameter, for example, modulus(0, 3).
  Assert: Check that the returned value is zero, as the modulus of zero by any number is zero.
Validation:
  This test is important to ensure that the function correctly handles the case where the first parameter is zero.

"""

# ********RoostGPT********
import pytest
from calc import SimpleCalculator

class Test_SimpleCalculatorModulus:

    def test_modulus_positive_integers(self):
        calculator = SimpleCalculator()
        result = calculator.modulus(10, 3)
        assert result == 1, "Modulus of two positive integers is incorrect"

    def test_modulus_negative_integers(self):
        calculator = SimpleCalculator()
        result = calculator.modulus(-10, -3)
        assert result == -1, "Modulus of two negative integers is incorrect"

    def test_modulus_zero_second_parameter(self):
        calculator = SimpleCalculator()
        result = calculator.modulus(10, 0)
        assert result == "Cannot perform modulus by zero", "Modulus by zero is not handled correctly"

    def test_modulus_zero_first_parameter(self):
        calculator = SimpleCalculator()
        result = calculator.modulus(0, 3)
        assert result == 0, "Modulus of zero by any number is incorrect"
