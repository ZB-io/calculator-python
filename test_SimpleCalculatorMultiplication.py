# ********RoostGPT********
"""
Test generated by RoostGPT for test python-calc-claude using AI Type Claude AI and AI Model claude-3-5-sonnet-20241022

ROOST_METHOD_HASH=multiplication_b85031f6ad
ROOST_METHOD_SIG_HASH=multiplication_c14ad406cb


Scenario 1: Basic Positive Number Multiplication
Details:
  TestName: test_positive_number_multiplication
  Description: Verify that the function correctly multiplies two positive numbers and returns their product.
Execution:
  Arrange: No specific setup required
  Act: Call multiplication(4, 5)
  Assert: Verify that the result equals 20
Validation:
  This test ensures the core functionality of multiplying two positive integers works as expected, which is the primary use case for the function.

"""

# ********RoostGPT********
import pytest
from calc import SimpleCalculator

class TestSimpleCalculatorMultiplication:
    def test_positive_number_multiplication(self):
        """Test multiplication of two positive numbers."""
        # Arrange
        calculator = SimpleCalculator()
        
        # Act
        result = calculator.multiplication(4, 5)
        
        # Assert
        assert result == 20, f"Expected 4 * 5 = 20, but got {result}"
        
    def test_negative_number_multiplication(self):
        """Test multiplication of negative numbers."""
        # Arrange
        calculator = SimpleCalculator()
        
        # Act & Assert
        assert calculator.multiplication(-3, 4) == -12, "Failed multiplying negative and positive numbers"
        assert calculator.multiplication(-2, -5) == 10, "Failed multiplying two negative numbers"
        
    def test_zero_multiplication(self):
        """Test multiplication with zero."""
        # Arrange
        calculator = SimpleCalculator()
        
        # Act & Assert
        assert calculator.multiplication(0, 5) == 0, "Multiplication with zero should return 0"
        assert calculator.multiplication(10, 0) == 0, "Multiplication with zero should return 0"
        
    def test_large_numbers_multiplication(self):
        """Test multiplication of large numbers."""
        # Arrange
        calculator = SimpleCalculator()
        
        # Act
        result = calculator.multiplication(999999, 999999)
        
        # Assert
        assert result == 999998000001, "Failed multiplying large numbers"
        
    def test_decimal_numbers_multiplication(self):
        """Test multiplication of decimal numbers."""
        # Arrange
        calculator = SimpleCalculator()
        
        # Act
        result = calculator.multiplication(2.5, 2.0)
        
        # Assert
        assert result == 5.0, "Failed multiplying decimal numbers"
        
    def test_type_error_handling(self):
        """Test that TypeError is raised for invalid input types."""
        # Arrange
        calculator = SimpleCalculator()
        
        # Act & Assert
        with pytest.raises(TypeError):
            calculator.multiplication("2", 3)
        
        with pytest.raises(TypeError):
            calculator.multiplication([], 5)

def print_test_info(test_name, result):
    """Helper function to print test information."""
    print(f"Running test: {test_name}")
    print(f"Result: {result}")
    print("-" * 50)

