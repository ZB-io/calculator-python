# ********RoostGPT********
"""
Test generated by RoostGPT for test python-calc-claude using AI Type Claude AI and AI Model claude-3-5-sonnet-20241022

ROOST_METHOD_HASH=multiplication_b85031f6ad
ROOST_METHOD_SIG_HASH=multiplication_c14ad406cb


Scenario 1: Basic Positive Number Multiplication
Details:
  TestName: test_multiply_positive_numbers
  Description: Verify that the function correctly multiplies two positive numbers, testing the core functionality of the multiplication operation.
Execution:
  Arrange: No specific setup required
  Act: Call multiplication(4, 5)
  Assert: Verify that the result equals 20
Validation:
  This test ensures the basic multiplication operation works correctly for positive integers, which is the fundamental use case for this function.

"""

# ********RoostGPT********
import pytest
from calc import SimpleCalculator

class TestSimpleCalculatorMultiplication:
    def test_multiply_positive_numbers(self):
        """Test multiplication of two positive numbers."""
        # Arrange
        calculator = SimpleCalculator()
        
        # Act
        result = calculator.multiplication(4, 5)
        
        # Assert
        assert result == 20, f"Expected 4 * 5 = 20, but got {result}"
        
    def test_multiply_negative_numbers(self):
        """Test multiplication of two negative numbers."""
        # Arrange
        calculator = SimpleCalculator()
        
        # Act
        result = calculator.multiplication(-3, -2)
        
        # Assert
        assert result == 6, f"Expected -3 * -2 = 6, but got {result}"
        
    def test_multiply_zero(self):
        """Test multiplication with zero."""
        # Arrange
        calculator = SimpleCalculator()
        
        # Act & Assert
        assert calculator.multiplication(0, 5) == 0, "Multiplication with zero should return 0"
        assert calculator.multiplication(5, 0) == 0, "Multiplication with zero should return 0"
        
    def test_multiply_floating_points(self):
        """Test multiplication with floating point numbers."""
        # Arrange
        calculator = SimpleCalculator()
        
        # Act
        result = calculator.multiplication(2.5, 2.0)
        
        # Assert
        assert abs(result - 5.0) < 1e-10, f"Expected 2.5 * 2.0 = 5.0, but got {result}"
        
    def test_multiply_large_numbers(self):
        """Test multiplication with large numbers."""
        # Arrange
        calculator = SimpleCalculator()
        
        # Act
        result = calculator.multiplication(1000000, 1000000)
        
        # Assert
        assert result == 1000000000000, f"Expected 1000000 * 1000000 = 1000000000000, but got {result}"

    def test_multiply_positive_negative(self):
        """Test multiplication of positive and negative numbers."""
        # Arrange
        calculator = SimpleCalculator()
        
        # Act
        result = calculator.multiplication(-5, 3)
        
        # Assert
        assert result == -15, f"Expected -5 * 3 = -15, but got {result}"

    @pytest.mark.parametrize("num1, num2, expected", [
        (1, 1, 1),
        (-1, -1, 1),
        (0, 1, 0),
        (100, 0.5, 50),
        (-2, 3, -6)
    ])
    def test_multiplication_parametrized(self, num1, num2, expected):
        """Test multiplication with various input combinations using parametrize."""
        # Arrange
        calculator = SimpleCalculator()
        
        # Act
        result = calculator.multiplication(num1, num2)
        
        # Assert
        assert abs(result - expected) < 1e-10, f"Expected {num1} * {num2} = {expected}, but got {result}"

    def test_multiply_type_error(self):
        """Test multiplication with invalid input types."""
        # Arrange
        calculator = SimpleCalculator()
        
        # Act & Assert
        with pytest.raises(TypeError):
            calculator.multiplication("2", 3)
        
        with pytest.raises(TypeError):
            calculator.multiplication([], 3)

